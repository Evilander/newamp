<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- The renderer compiles MilkDrop preset math via the Function constructor
       (Butterchurn / 'unsafe-eval') and runs the sql.js WASM library, both of
       which require JIT under the hardened runtime. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <!-- ffmpeg-static ships a prebuilt binary that NewAmp spawns for transcode /
       WAV export / ReplayGain; allow loading it under the hardened runtime. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
