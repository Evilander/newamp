# Decisions — newamp-master-plan-fable5

One line per decision as the executor makes them. Format: `YYYY-MM-DD — decision — why`.

- 2026-07-02 — Clip Studio pattern = WebCodecs VP9/Opus ring + Mediabunny mux + ffmpeg h264_nvenc finish — MediaRecorder chunk-dropping yields unplayable WebM (cluster/header dependency); stock Electron ships no H.264 WebCodecs encode (electron#38213); bundled ffmpeg verified to carry libx264 + h264_nvenc + aac.
- 2026-07-02 — Bit-perfect path = vendored miniaudio in a custom N-API addon, NOT audify/RtAudio — RtAudio's WASAPI backend hardcodes AUDCLNT_SHAREMODE_SHARED with exclusive as an unimplemented TODO (verified in master 2026-07-02); docs/audio-quality.md's audify plan is wrong and must be corrected.
- 2026-07-02 — Ask Your Library compiles to SmartPlaylistRuleInput + post-fetch DNA re-rank, not a live Tags-DSL executor — the SQL path + scoring already exist end-to-end (smart:run); re-rank via buildDnaIndex recovers DNA expressiveness for ordering at a fraction of the work.
- 2026-07-02 — NewAmp Remote extends radio-brain (one port, one secured server) and its token gate lands FIRST — the server today is 0.0.0.0 with zero auth, already serving library audio LAN-wide.
- 2026-07-02 — Sequence 1→5 = Clip Studio, Wrapped Live, Remote, Ask, Bit-Perfect; bit-perfect go/no-go spike runs in parallel with #1 — retire the only native-code risk before its slot; mux infra from #1 feeds #2; December deadline holds #2's place.
- 2026-07-02 — Bit-perfect spike verdict: GO — vendored miniaudio 0.11.21 + node-addon-api built with VS2022 Community (node-gyp needs vcvars64; its PowerShell VS discovery fails on this box) and opened WASAPI EXCLUSIVE on the Focusrite USB interface at 48k AND 44.1k, rendering frames both times (spike at B:\projects\claude\newamp-native-spike). Full Feature 5 integration proceeds next release.
- 2026-07-02 — Features 1-4 shipped in one pass (v1.16.0): Clip Studio (WebCodecs ring, smoke:clip-replay), Wrapped Live (smoke:wrapped-live), Remote (token gate incl. pre-existing exposure fix, smoke:remote), Ask (local compiler 10/10, test:query-intent). Ask's LLM tier deferred as planned — local vocabulary hit the acceptance bar.
- 2026-07-03 — Bit-perfect addon built for real: native/newamp-audio (miniaudio 0.11.x vendored, SPSC lock-free ring, honest requested-vs-internal format report, NAPI_VERSION=8) — verified loading + device enum + probe inside Electron 42's Node 24.15 (N-API 10/ABI 146) with NO electron-rebuild; built against ambient Node 25 headers via vcvars64 (scripts/build-native.mjs; node-gyp VS auto-discovery broken on this box).
- 2026-07-03 — Exclusive negotiation is probe-driven: choose device-NATIVE format/rate from probeDevice(); if source rate unsupported, resample explicitly in ffmpeg (soxr) and report honestly (badge silver, not gold) — Focusrite probe showed miniaudio silently resamples 44.1k→48k in exclusive if you don't match native (internalSampleRate exposes it).
- 2026-07-03 — Renderer seam = ExternalTransport facade inside AudioEngine (play/pause/seek/stop delegate; state pushed back via engine.patch) so usePlayerStore + every consumer (shell notify, scrobble, sleep, queue advance) need zero transport changes; viz stays alive via ExternalAnalysisTap (spec-faithful Blackman/FFT/dB AnalyserNode emulation at engine get*Data choke point, fed 30Hz PCM from main).
- 2026-07-03 — Feature 5 pre-release adversarial review (4 lenses → 17 raw findings → 11 confirmed after 2-verifier refutation, all fixed): 1 critical (stuck ended:true → cascading queue-advance at gapless boundaries; fixed at four layers — boundary event isolation + ended timer stop in driver, endedPending position suppression in bridge, synchronous ended-clear + playSeq staleness guard in engine, lastEndedKey dedup in store), plus IPC play-ordering, disable-while-paused dead Play button, .mka false-lossless, unknown-rate/channels honesty flags (new channelsUnknown field), device-lost crash guard, chained-ack tap blink, torn stats snapshots, smoke:exclusive-ui gate wiring.
